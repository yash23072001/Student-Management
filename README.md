# Student-Management
A secure RESTful backend application built using Spring Boot for managing student data. The application implements JWT-based authentication and authorization using Spring Security. Users can sign up, log in, and access protected APIs using JWT tokens. Student records are managed via full CRUD operations, with data persisted in a PostgreSQL database.
